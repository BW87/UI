<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test">
        <activity android:name=".MedicalActivity"></activity>
        <activity android:name=".LanguageSettingActivity" />
        <activity android:name=".AlarmSettingActivity" />
        <activity android:name=".ContactEditActivity" />
        <activity android:name=".ContactAddActivity" />
        <activity android:name=".ContactRBGActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".EmergencyContactActivity" />
        <activity android:name=".RiskInfoAndWeatherActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>